<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <style type="text/css">
            p {
                margin-top: 10px;
                height: 0px;
                font-size: 20px;
            }
            th p {
                float: left;
            }
            .content {
                background-color: #fff;
                margin: 0 1em;
                padding-left: 1em;
            }
            .right {
                float: right;
                font-size: 20px;    
            }
            .centrar {
                text-align: center !important;
                float: none !important;
            }
            .text-right {
                text-align: right;
            }
            .text-long {
                font-size: 20px;
                font-weight: bold;
            }
        </style>
    </head>
    <body class="focusedform">
        <div class="right">{{ fecha|localizeddate('full', 'none') }}</div>
        <br>
        <div class="content">
            <div class="centrar">
                <h2 style="color: #4285F4; font-size: 35px">Listado Inscripción II Juegos Deportivos Nacionales de Profesionales en Ciencias Económicas Puerto Iguazu 2018</h2>
            </div>
            <div>
                <h3>PROFESIONALES INSCRIPTOS:</h3>
                <hr>
                {% if entities|length > 0 %}
                    <table style="width:100%; text-align:center;" border="0">
                        <tr style="background-color: #e0e0e0;">
                            <th colspan="4"><strong>Nro. Matricula</strong></th>
                            <th colspan="4"><strong>Apellido, Nombre</strong></th>
                            <th colspan="4"><strong>Dni</strong></th>
                            <th colspan="4"><strong>Fecha Generado</strong></th>
                            <th colspan="4"><strong>Importe</strong></th>
                            <th colspan="4"><strong>Estado</strong></th>
                        </tr>
                        {% for key, entity in entities %}
                            <tr>
                                <td colspan="4">{{ entity.matricula }}</td>
                                <td colspan="4">{{ entity.nombre }}</td>
                                <td colspan="4">{{ entity.dni }}</td>
                                <td colspan="4">{{ entity.fecha|date("d/m/Y") }}</td>
                                <td colspan="4">${{ entity.importe }}</td>
                                <td colspan="4">
                                    {% if entity.estado == 0 %}
                                        Generado
                                    {% elseif entity.estado == 1 %}
                                        Señado
                                    {% else %}
                                        Pagado
                                    {% endif %}
                                </td>
                            </tr>
                        {% endfor %}
                    </table>
                {% endif %}
            </div>
            <br></br> 
            <div class="right">
                <table style="width:100%" border="0">
                    <tr><td colspan="4"><p></p></td></tr>
                    <tr>
                        <td colspan="4"><p>.................................</p></td>
                    </tr>
                    <tr>
                        <td colspan="4"><p><strong>Firma y Aclaración</strong></p></td>
                    </tr>
                </table>  
            </div>
        </div>  
    </body>
</html>